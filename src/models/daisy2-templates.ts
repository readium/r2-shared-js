export interface SmilTemplate {
    audioEl: any;
    parSrc: string;
}

export interface TempTemplate {
    playOrder: any;
    smilData: any;
    tag: string;
    text: string;
}

export interface NccTemplate {
    meta: any;
    nccItems: any;
}

export interface OpfItemsTemplate {
    manifest: any[];
    spine: any[];
}
